import nextI18NextConfig from './next-i18next.config.js'

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: i18n routing is not directly configured here for App Router
  // next-i18next handles locale detection and loading
};

export default nextConfig;
